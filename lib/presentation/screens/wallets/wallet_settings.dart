import 'package:flutter/material.dart';
import 'components/wallet_settings_body.dart';

class WalletSettings extends StatelessWidget {
  @override
  Widget build(BuildContext context) {
    return Scaffold(
      body: WalletSettingBody(),
    );
  }
}
